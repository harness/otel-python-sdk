# Publishing moved to Traceableai/pythonagent (private GAR).
# This workflow is kept for backward compatibility; use publish-dispatcher.yaml.
name: Publish (deprecated)

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "harness-sdk is published via Traceableai/pythonagent."
          echo "Tag harness/otel-python-sdk to run publish-dispatcher.yaml, or trigger"
          echo "otel-sdk-publish-dispatcher.yaml in pythonagent manually."
